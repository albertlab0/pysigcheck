[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pysigcheck"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "pefile",
    "signify",
    "pycryptodome",
]

[project.scripts]
pysigcheck = "pysigcheck:main"
